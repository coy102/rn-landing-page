import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
  cardImage: {
    alignSelf: 'center',
    height: 200,
    width: '100%',
    borderRadius: 4,
  },
})

export default style
